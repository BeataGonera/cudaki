const ButtonCTABlue = ({ label }: { label: string }) => {
  return (
    <button className="bg-[#6FCFEB] rounded-full w-full h-full lg:w-[160px] font-bold text-[14px]">
      {label}
    </button>
  );
};

export default ButtonCTABlue;
